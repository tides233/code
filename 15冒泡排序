#include<stdio.h>
//这是一个冒泡排序
void bubble(int arr[], int sz)
{
	int i = 0;
	for (i = 0; i < sz; i++)
	{/*生成进行多少轮冒泡*/
		int j = 0;
		for (j = 0; j < sz - 1 - i; j++)/*sz-1是定值，再减去i即可实现第一次9第二次8*/
		{/*每一次冒泡比较多少对数；第一次9对，第二次8对......*/
			if (arr[j]>arr[j+1])
			{
				int t = arr[j];
				arr[j] = arr[j + 1];
				arr[j + 1] = t;
			}
		}
	}

}
int main()
{

	int arr[] = { 9,8,7,6,5,4,3,2,1,0 };
	/*冒泡排序：第一个数与第二个数比较，大的放后面，然后第二个数再与第三个数比较，最终结果为{8,7,6,5,4,3,2,1,0,9}
		此为第一轮冒泡
		第二轮重复第一轮的操作得到{7,6,5,4,3,2,1,0,8,9}
		如此循环则可得到升序*/
	/*第一次10在后面，第二次9，第三次8....第9次2，此时已经满足升序，故10个元素需要进行轮冒泡；
		第一轮冒泡10个元素两两比较需要9次，第二轮8次....*/
	int i = 0;
	int sz = sizeof(arr) / sizeof(arr[0]);
	bubble(arr,sz);
	for (i = 0; i < sz; i++)
	{
		printf("%d ", arr[i]);
		
	}


	return 0;
}
